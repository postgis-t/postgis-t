# postgis-t extension
# which will be installed into prefix/share/extension

comment = 'Spatial-Temporal Geographic Objects'
default_version = '0.2.0'
module_pathname = '$libdir/postgist'
relocatable = true
requires = postgis
